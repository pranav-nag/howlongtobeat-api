# Port for the API server
PORT=3000

# Cache TTL in seconds (default: 86400 / 24 hours)
CACHE_TTL_SECONDS=86400

# Force refresh cooldown in milliseconds (default: 5000 / 5 seconds)
FORCE_COOLDOWN_MS=5000
